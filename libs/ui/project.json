{
  "name": "ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/ui/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project ui --web",
  "targets": {
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm install --prefix libs/ui"]
      }
    }
  }
}
